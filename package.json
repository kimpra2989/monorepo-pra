{
  "name": "fc3",
  "version": "0.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": " nx run-many -t=build --graph"
  },
  "packageManager": "yarn@4.6.0",
  "devDependencies": {
    "nx": "^20.3.3"
  }
}
